// cypress/plugins/index.js
// Cypress will call this function when it launches. You can hook into task events here.
// For now, just return the config unchanged.

module.exports = (on, config) => {
  return config;
};
